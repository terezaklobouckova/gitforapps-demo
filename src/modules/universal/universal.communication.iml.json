{
    "url": "/locations/{{parameters.locationId}}/rooms",
    "method": "POST",
    "body": {
        "name": "{{parameters.name}}"
    },
    "headers": {},
    "response": {
        "output": "{{parseTimestamps(body)}}"
    }
}