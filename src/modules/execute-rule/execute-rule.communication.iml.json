{
    "url": "/rules/execute/{{parameters.ruleId}}",
    "method": "POST",
    "qs": {
        "locationId": "{{parameters.locationId}}"
    },
    "response": {
        "output": "{{undefined}}"
    }
}