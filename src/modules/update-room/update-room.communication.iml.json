{
    "url": "/locations/{{parameters.locationId}}/rooms/{{parameters.roomId}}",
    "method": "PUT",
    "body": {
        "name": "{{parameters.name}}"
    },
    "headers": {},
    "response": {
        "output": "{{parseTimestamps(body)}}"
    }
}