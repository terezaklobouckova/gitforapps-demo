{
    "url": "/rules",
    "method": "GET",
    "qs": {
        "locationId": "{{parameters.locationId}}"
    },
    "response": {
        "iterate": "{{body.items}}",
        "output": {
            "label": "{{item.name}} ({{item.status}})",
            "value": "{{item.id}}"
        }
    }
}