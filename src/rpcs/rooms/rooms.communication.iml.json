{
    "url": "/locations/{{parameters.locationId}}/rooms",
    "method": "GET",
    "response": {
        "iterate": "{{body.items}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.roomId}}"
        }
    }
}