{
    "url": "/scenes",
    "method": "GET",
    "qs": {
        "locationId": "{{parameters.locationId}}"
    },
    "response": {
        "iterate": "{{body.items}}",
        "output": {
            "label": "{{item.sceneName}}",
            "value": "{{item.sceneId}}"
        }
    }
}